const music=document.querySelector('#home-audio'),button=document.querySelector('.music-toggle'),state=document.querySelector('.music-state'),symbol=document.querySelector('.music-symbol');
function update(playing,message){button.classList.toggle('is-playing',playing);button.setAttribute('aria-pressed',String(playing));const label=playing?'暂停背景音乐':'播放背景音乐';button.setAttribute('aria-label',label);button.title=label;symbol.innerHTML=playing?'<svg viewBox="0 0 24 24" fill="currentColor"><rect x="6" y="5" width="4" height="14" rx="1"/><rect x="14" y="5" width="4" height="14" rx="1"/></svg>':'<svg viewBox="0 0 24 24" fill="currentColor"><path d="M6 3L21 12 6 21Z"/></svg>';state.textContent=message;}
async function start(){try{await music.play();}catch{update(false,'点击唱片开始播放');}}
music.volume=.3;music.addEventListener('play',()=>update(true,'正在播放，点击暂停'));music.addEventListener('pause',()=>update(false,'已暂停，点击播放'));music.addEventListener('error',()=>update(false,'音乐暂时无法加载，请刷新后重试'));button.addEventListener('click',()=>music.paused?start():music.pause());update(false,'点击唱片开始播放');start();
